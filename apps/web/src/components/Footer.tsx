export default function Footer() {
  return (
    <footer className="border-t border-border px-4 py-8 text-sm text-muted-foreground">
      <div className="mx-auto flex w-full max-w-7xl flex-col gap-2 sm:flex-row sm:items-center sm:justify-between">
        <p className="m-0">ProxyKey records agent mandates, execution, and receipts on Casper Testnet.</p>
        <p className="m-0">No agent receives the user private key.</p>
      </div>
    </footer>
  )
}
