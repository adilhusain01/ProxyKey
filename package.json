{
  "name": "proxykey",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.7.0",
  "scripts": {
    "dev": "turbo dev",
    "build": "turbo build",
    "test": "turbo test",
    "typecheck": "turbo typecheck",
    "lint": "turbo lint",
    "contracts:test": "cargo test --manifest-path contracts/agent-mandates/Cargo.toml"
  },
  "devDependencies": {
    "@playwright/test": "^1.61.1",
    "turbo": "^2.6.3",
    "typescript": "^6.0.2"
  }
}
